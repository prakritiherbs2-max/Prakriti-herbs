services:
  - type: web
    name: prakriti-herbs-api
    runtime: node
    buildCommand: pnpm install && pnpm run build
    startCommand: node --enable-source-maps ./artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production